{
  "name": "RestServer",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "nodemon server.js",
    "start": "mkdir wallet && sudo chattr -R +a wallet/ && node server.js",
    "stop": "sudo chattr -R -a wallet/ && rf -rf wallet/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "fabric-ca-client": "^2.1.0",
    "fabric-network": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
