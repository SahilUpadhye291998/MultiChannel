const { Gateway, Wallets } = require("fabric-network");
const fs = require("fs");
const path = require("path");

async function getContractForCustomer(name) {}

module.exports = {
  getContractForCustomer,
};
